---
phase: 33-reflection-based-handlers
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - unreal-plugin/MCPBridge/Source/MCPBridgeEditor/Private/MCPAudioCommands.h
  - unreal-plugin/MCPBridge/Source/MCPBridgeEditor/Private/MCPAudioCommands.cpp
  - unreal-plugin/MCPBridge/Source/MCPBridgeEditor/Private/MCPAnimationCommands.h
  - unreal-plugin/MCPBridge/Source/MCPBridgeEditor/Private/MCPAnimationCommands.cpp
  - unreal-plugin/MCPBridge/Source/MCPBridgeEditor/Private/MCPWorldPartitionCommands.h
  - unreal-plugin/MCPBridge/Source/MCPBridgeEditor/Private/MCPWorldPartitionCommands.cpp
autonomous: true
requirements:
  - REFLECT-01

must_haves:
  truths:
    - "MCPAudioCommands compiles without MetasoundSource.h, MetasoundFrontendDocument.h, or MetasoundDocumentInterface.h"
    - "MCPAnimationCommands compiles without Retargeter/IKRetargeter.h, Retargeter/IKRetargetChainMapping.h, or any IKRig headers"
    - "MCPWorldPartitionCommands compiles without DataLayer/DataLayerEditorSubsystem.h or any WorldPartitionEditor/DataLayerEditor headers"
    - "All three handlers return module_not_available for their optional module when it is not loaded"
    - "Core animation handlers (AnimBP, Montage, BlendSpace, StateMachine) still use direct includes since Animation module is in Build.cs"
  artifacts:
    - path: "unreal-plugin/MCPBridge/Source/MCPBridgeEditor/Private/MCPAudioCommands.cpp"
      provides: "Reflection-based audio handlers"
      contains: "FindObject<UClass>"
    - path: "unreal-plugin/MCPBridge/Source/MCPBridgeEditor/Private/MCPAnimationCommands.cpp"
      provides: "Reflection-based animation handlers"
      contains: "FindObject<UClass>"
    - path: "unreal-plugin/MCPBridge/Source/MCPBridgeEditor/Private/MCPWorldPartitionCommands.cpp"
      provides: "Reflection-based world partition handlers"
      contains: "FindObject<UClass>"
  key_links:
    - from: "MCPAudioCommands.cpp"
      to: "ReflectionHelpers.h"
      via: "include"
      pattern: '#include "ReflectionHelpers.h"'
---

<objective>
Convert 3 medium-to-complex handlers (Audio, Animation, WorldPartition) from direct-include to reflection-based approach.

Purpose: These handlers (624-839 lines) have mixed complexity -- some handlers within each file use core APIs and others use optional plugin APIs. The conversion must split: keep direct includes for core functionality, add reflection only for optional plugin types.

Output: 3 converted handler pairs (.h/.cpp) in Private/ (not Optional/).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/33-reflection-based-handlers/33-CONTEXT.md

Read these disabled reference files before implementing:
@unreal-plugin/MCPBridge/Source/MCPBridgeEditor/Private/Optional/MCPAudioCommands.cpp.disabled
@unreal-plugin/MCPBridge/Source/MCPBridgeEditor/Private/Optional/MCPAudioCommands.h.disabled
@unreal-plugin/MCPBridge/Source/MCPBridgeEditor/Private/Optional/MCPAnimationCommands.cpp.disabled
@unreal-plugin/MCPBridge/Source/MCPBridgeEditor/Private/Optional/MCPAnimationCommands.h.disabled
@unreal-plugin/MCPBridge/Source/MCPBridgeEditor/Private/Optional/MCPWorldPartitionCommands.cpp.disabled
@unreal-plugin/MCPBridge/Source/MCPBridgeEditor/Private/Optional/MCPWorldPartitionCommands.h.disabled

Read the proven reflection pattern:
@unreal-plugin/MCPBridge/Source/MCPBridgeEditor/Private/Optional/MCPMotionDesignCommands.cpp.disabled

Read Build.cs:
@unreal-plugin/MCPBridge/Source/MCPBridgeEditor/MCPBridgeEditor.Build.cs

<interfaces>
Build.cs allowed modules:
Core, CoreUObject, Engine, UnrealEd, EditorSubsystem, Json, SlateCore, InputCore,
AssetTools, AssetRegistry, LevelEditor, PhysicsCore, Kismet, BlueprintGraph,
LevelSequence, LevelSequenceEditor, MovieScene, MovieSceneTracks, EnhancedInput,
AIModule, NavigationSystem, GameplayTags, GameplayTasks, MCPBridgeRuntime

ReflectionHelpers.h MCPReflect namespace:
```cpp
namespace MCPReflect {
  bool CheckModuleLoaded(const TCHAR* ModuleName);
  UClass* FindClassByPath(const TCHAR* ClassPath);
  FString GetStringProperty(UObject* Obj, FName PropName);
  double GetFloatProperty(UObject* Obj, FName PropName);
  int64 GetIntProperty(UObject* Obj, FName PropName);
  bool GetBoolProperty(UObject* Obj, FName PropName, bool& OutValue);
  UObject* GetObjectProperty(UObject* Obj, FName PropName);
  FString ExportPropertyAsString(UObject* Obj, FName PropName);
  bool SetPropertyFromString(UObject* Obj, FName PropName, const FString& Value);
  FString BuildSuccessResponse(const FString& CorrId, TSharedPtr<FJsonObject> Data);
  FString BuildErrorResponse(const FString& CorrId, const FString& Error);
  void SendModuleNotAvailable(FMCPResponseSender SendResponse, const FString& CorrId, const FString& ModuleName);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert MCPAudioCommands to reflection-based</name>
  <files>
    unreal-plugin/MCPBridge/Source/MCPBridgeEditor/Private/MCPAudioCommands.h,
    unreal-plugin/MCPBridge/Source/MCPBridgeEditor/Private/MCPAudioCommands.cpp
  </files>
  <action>
Read the .disabled reference file fully. It has 4 handlers: audio.list, audio.metasound, audio.soundcue, audio.insights (624 lines).

Analyze which includes are optional vs. core:
- CORE (Engine module, can keep): "Sound/SoundWave.h", "Sound/SoundCue.h", "Sound/SoundNode.h", "Sound/SoundNodeWavePlayer.h", "Sound/SoundNodeModulator.h", "Sound/SoundNodeAttenuation.h", "Sound/SoundAttenuation.h", "Engine/Attenuation.h", "AudioDevice.h", "AudioDeviceHandle.h"
- OPTIONAL (MetasoundEngine/MetasoundFrontend modules, MUST REMOVE): "MetasoundSource.h", "MetasoundFrontendDocument.h", "MetasoundDocumentInterface.h"
- CORE: AssetRegistry headers, Editor.h, Modules/ModuleManager.h, JSON headers

Conversion strategy:
- audio.list: Lists all sound assets. Uses IAssetRegistry (core) to find assets by class name. For MetaSound assets, instead of checking IsA(UMetaSoundSource::StaticClass()), use FindObject<UClass>(nullptr, TEXT("/Script/MetasoundEngine.MetaSoundSource")) to get the class, then check IsA(MetaSoundClass). If class is nullptr (module not loaded), simply skip MetaSound assets in the listing. Still list SoundWave and SoundCue assets (these are core).
- audio.metasound: Fully optional. Check MCPReflect::CheckModuleLoaded(TEXT("MetasoundEngine")). If false, return module_not_available. Find the MetaSoundSource class via FindObject<UClass>. Load the asset as UObject* via StaticLoadObject using the found UClass. Read graph nodes, inputs, outputs via FProperty iteration on the asset object. The MetaSound document interface properties (nodes, connections, inputs, outputs) are exposed as UPROPERTY and accessible via reflection.
- audio.soundcue: Uses core SoundCue API (USoundCue, USoundNode, etc.). Can use direct includes -- no optional modules involved. Keep implementation identical to .disabled.
- audio.insights: Uses FAudioDevice which is core Engine. Check if the audio.insights handler uses any MetaSound-specific types. If it only uses FAudioDevice/UAudioComponent (core), keep direct. If it references MetaSound monitoring, guard those paths with module checks.

Create .h identical to .disabled.
  </action>
  <verify>
    <automated>grep -c "MetasoundSource.h\|MetasoundFrontendDocument.h\|MetasoundDocumentInterface.h" unreal-plugin/MCPBridge/Source/MCPBridgeEditor/Private/MCPAudioCommands.cpp | xargs test 0 -eq</automated>
  </verify>
  <done>MCPAudioCommands .h/.cpp exist in Private/. No MetaSound module headers included. audio.soundcue and audio.insights use core APIs directly. audio.metasound uses reflection. audio.list uses reflection for MetaSound class detection. Returns module_not_available for MetaSound-specific queries when module is absent.</done>
</task>

<task type="auto">
  <name>Task 2: Convert MCPAnimationCommands to reflection-based</name>
  <files>
    unreal-plugin/MCPBridge/Source/MCPBridgeEditor/Private/MCPAnimationCommands.h,
    unreal-plugin/MCPBridge/Source/MCPBridgeEditor/Private/MCPAnimationCommands.cpp
  </files>
  <action>
Read the .disabled reference file fully. It has handlers for animation inspection (660 lines). Per CONTEXT.md, only the IKRig retarget mapping handler needs reflection. AnimBP, Montage, BlendSpace, StateMachine all use core Animation module headers.

Analyze includes:
- CORE (Engine module): "Animation/AnimBlueprint.h", "Animation/AnimMontage.h", "Animation/BlendSpace.h", "Animation/BlendSpace1D.h", "Animation/AnimSequence.h"
- CORE (Kismet/BlueprintGraph -- in Build.cs): "AnimGraphNode_StateMachine.h", "AnimationStateMachineGraph.h", "AnimStateNode.h", "AnimStateTransitionNode.h"
- CORE: AssetRegistry headers, JSON headers
- OPTIONAL (IKRig module, MUST REMOVE): "Retargeter/IKRetargeter.h", "Retargeter/IKRetargetChainMapping.h"

Conversion strategy:
- Handlers that use AnimBlueprint, AnimMontage, BlendSpace, AnimSequence, state machine graph nodes: keep direct includes (all from core Engine/Kismet modules). Implementation stays identical to .disabled.
- IK Retarget handler (anim.retarget or similar): Check MCPReflect::CheckModuleLoaded(TEXT("IKRig")). If false, return module_not_available. Find UIKRetargeter class via FindObject<UClass>(nullptr, TEXT("/Script/IKRig.IKRetargeter")). Load asset as UObject*. Read chain mappings, source/target skeletons via FProperty iteration. FArrayProperty for chain mapping arrays, FStructProperty for individual mappings. Read source chain name, target chain name via FNameProperty on the struct elements.

Important: AnimGraphNode_StateMachine is from the AnimGraph module. Check if AnimGraph is in Build.cs. It is NOT explicitly listed, but it comes transitively through BlueprintGraph or Kismet. Verify by checking if the .disabled file uses these directly. If the AnimGraph headers are available through existing transitive deps, keep them. If not, guard those handlers too with a module check, or use reflection to access state machine graph nodes.

Actually, re-examine: The disabled file includes "AnimGraphNode_StateMachine.h" which is from the AnimGraph editor module. AnimGraph may be available through BlueprintGraph dependency. If it compiles without adding AnimGraph to Build.cs, keep it. If there is any doubt, use FindObject<UClass>(nullptr, TEXT("/Script/AnimGraph.AnimGraphNode_StateMachine")) instead.

Create .h identical to .disabled.
  </action>
  <verify>
    <automated>grep -c "IKRetargeter.h\|IKRetargetChainMapping.h" unreal-plugin/MCPBridge/Source/MCPBridgeEditor/Private/MCPAnimationCommands.cpp | xargs test 0 -eq</automated>
  </verify>
  <done>MCPAnimationCommands .h/.cpp exist in Private/. No IKRig headers included. Core animation handlers (AnimBP, Montage, BlendSpace, StateMachine) use direct includes. IK Retarget handler uses FindObject/FProperty reflection. Returns module_not_available for retarget queries when IKRig module is absent.</done>
</task>

<task type="auto">
  <name>Task 3: Convert MCPWorldPartitionCommands to reflection-based</name>
  <files>
    unreal-plugin/MCPBridge/Source/MCPBridgeEditor/Private/MCPWorldPartitionCommands.h,
    unreal-plugin/MCPBridge/Source/MCPBridgeEditor/Private/MCPWorldPartitionCommands.cpp
  </files>
  <action>
Read the .disabled reference file fully. It has handlers for world partition and data layers (839 lines).

Analyze includes:
- NEED TO VERIFY (Engine vs. WorldPartitionEditor module):
  - "WorldPartition/WorldPartition.h" -- This is in Engine module (core). KEEP.
  - "WorldPartition/WorldPartitionSubsystem.h" -- Engine module. KEEP.
  - "WorldPartition/WorldPartitionStreamingSource.h" -- Engine module. KEEP.
  - "WorldPartition/WorldPartitionRuntimeHash.h" -- Engine module. KEEP.
  - "Components/WorldPartitionStreamingSourceComponent.h" -- Engine module. KEEP.
  - "WorldPartition/DataLayer/DataLayerInstance.h" -- Engine module. KEEP.
  - "WorldPartition/DataLayer/DataLayerInstanceWithAsset.h" -- Engine module. KEEP.
  - "WorldPartition/DataLayer/WorldDataLayers.h" -- Engine module. KEEP.
  - "WorldPartition/HLOD/HLODLayer.h" -- Engine module. KEEP.
- OPTIONAL (DataLayerEditor module, MUST REMOVE): "DataLayer/DataLayerEditorSubsystem.h"
- CORE: AssetRegistry headers, JSON headers, Editor.h, Engine/World.h, EngineUtils.h, GameFramework/Actor.h, Components/ActorComponent.h, Async/Async.h

Conversion strategy:
- Most WorldPartition types are in core Engine. The ONLY optional type is UDataLayerEditorSubsystem from the DataLayerEditor module.
- Handlers that use UWorldPartition, UWorldPartitionSubsystem, streaming sources, HLOD layers: keep direct includes. Implementation stays identical to .disabled.
- Handlers that use UDataLayerEditorSubsystem: Check MCPReflect::CheckModuleLoaded(TEXT("DataLayerEditor")). If false, return module_not_available for that specific handler. Find UDataLayerEditorSubsystem via FindObject<UClass>(nullptr, TEXT("/Script/DataLayerEditor.DataLayerEditorSubsystem")). Get subsystem instance from GEditor->GetEditorSubsystemBase(Class). Call methods (GetDataLayerInstances, SetDataLayerVisibility, etc.) via FindFunction + ProcessEvent.
- DataLayerInstance and WorldDataLayers are in core Engine -- only the DataLayerEditorSubsystem (editor-side operations) is optional.

Create .h identical to .disabled.
  </action>
  <verify>
    <automated>grep -c "DataLayerEditorSubsystem.h" unreal-plugin/MCPBridge/Source/MCPBridgeEditor/Private/MCPWorldPartitionCommands.cpp | xargs test 0 -eq</automated>
  </verify>
  <done>MCPWorldPartitionCommands .h/.cpp exist in Private/. No DataLayerEditor headers included. Core WorldPartition handlers use direct Engine APIs. DataLayer editor operations use reflection for UDataLayerEditorSubsystem. Returns module_not_available for data layer editing when DataLayerEditor module is absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON command -> handler | Untrusted asset_path from MCP client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-07 | Tampering | asset_path in audio/anim/worldpartition | mitigate | Preserve path validation from .disabled versions (FPackageName::IsValidLongPackageName, starts-with /Game/) |
| T-33-08 | Information Disclosure | Reflection error paths | mitigate | Return named error codes, not raw UE messages |
| T-33-09 | Denial of Service | WorldPartition cell enumeration | accept | Same iteration caps as .disabled versions |
</threat_model>

<verification>
All 3 converted handler .cpp files compile without optional plugin includes.
grep for removed optional headers returns 0 matches across all files.
Core functionality handlers (SoundCue, AnimBP, WorldPartition) still use direct includes for their core Engine types.
</verification>

<success_criteria>
MCPAudioCommands, MCPAnimationCommands, and MCPWorldPartitionCommands exist in Private/ with no optional plugin includes. MetaSound handler uses reflection. IKRetarget handler uses reflection. DataLayerEditor handler uses reflection. All other handlers within these files use core Engine APIs directly.
</success_criteria>

<output>
Create `.planning/phases/33-reflection-based-handlers/33-03-SUMMARY.md` when done
</output>
